This is a project in react that focuses on using magnifying glass, it was made using react and CSS

Logic :

- Static Props is for all the props that the user cannot interact with
- Planet is the component for all props that the user cannot interact with
- Map Is a component that draws all of the props using drawMap()
